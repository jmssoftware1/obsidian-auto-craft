import { useEffect, useState } from "react";
import heroImage from "@/assets/hero.jpg";
export function Hero() {
  const [mouse, setMouse] = useState({ x: 0, y: 0 });
  useEffect(() => { if (!window.matchMedia("(pointer: fine)").matches) return; const onMove = (e: MouseEvent) => setMouse({ x: e.clientX / innerWidth - .5, y: e.clientY / innerHeight - .5 }); window.addEventListener("mousemove", onMove, { passive: true }); return () => window.removeEventListener("mousemove", onMove); }, []);
  return <section id="top" className="hero relative min-h-[100svh] overflow-hidden bg-bg"><div className="absolute inset-0 lg:left-[31%]" style={{ transform: `translate3d(${mouse.x * -10}px, ${mouse.y * -6}px, 0) scale(1.035)`, transition: "transform 1.2s cubic-bezier(.16,1,.3,1)" }}><img src={heroImage} alt="Sedã preto sob iluminação lateral dramática em estúdio escuro" width={1408} height={1808} fetchPriority="high" className="h-full w-full object-cover object-center" /><div className="absolute inset-0 bg-[linear-gradient(90deg,rgba(9,9,10,1)_0%,rgba(9,9,10,.86)_20%,rgba(9,9,10,.24)_65%,rgba(9,9,10,.55)_100%)]" /><div className="absolute inset-0 bg-[linear-gradient(0deg,rgba(9,9,10,.85),transparent_35%)]" /></div><div className="hero-noise absolute inset-0 pointer-events-none" aria-hidden="true" /><div className="shell relative flex min-h-[100svh] flex-col justify-end pb-12 pt-32 lg:justify-center lg:pb-0"><div className="max-w-3xl"><div className="hero-kicker flex items-center gap-4"><span className="label text-accent">01 / 08</span><span className="h-px w-14 bg-accent/50" /><span className="label">Estética automotiva de precisão</span></div><h1 className="hero-title mt-7 max-w-4xl font-display text-[clamp(3.5rem,9.5vw,8.8rem)] font-extrabold uppercase leading-[0.82] tracking-[-0.055em]">O carro<br />não precisa<br /><span className="text-accent">parecer novo.</span></h1><div className="mt-9 flex max-w-xl flex-col gap-7 sm:flex-row sm:items-end sm:justify-between"><p className="max-w-sm font-mono text-[11px] leading-[1.8] tracking-[0.08em] text-ink-dim">Precisa parecer <span className="text-ink">exatamente como deveria</span>. Correção, proteção e acabamento com uma leitura obsessiva de cada superfície.</p><a href="#servicos" className="group inline-flex items-center gap-4 self-start label text-ink"><span className="grid h-11 w-11 place-items-center border border-accent text-accent transition-all duration-500 group-hover:bg-accent group-hover:text-bg">↓</span><span className="transition-colors group-hover:text-accent">Explorar</span></a></div></div></div><div className="absolute bottom-6 right-6 hidden lg:block"><span className="label text-ink/40">est. / conceito</span></div></section>;
}