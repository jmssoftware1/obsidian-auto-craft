import { useCallback, useEffect, useRef, useState } from "react";
import afterImage from "@/assets/after.jpg"; import beforeImage from "@/assets/before.jpg"; import { Reveal } from "@/components/Reveal"; import { SectionIndex } from "@/components/SectionIndex";
export function BeforeAfter(){const frameRef=useRef<HTMLDivElement|null>(null);const[position,setPosition]=useState(50);const dragging=useRef(false);const setFromClientX=useCallback((x:number)=>{const n=frameRef.current;if(!n)return;const r=n.getBoundingClientRect();setPosition(Math.min(100,Math.max(0,((x-r.left)/r.width)*100)))},[]);useEffect(()=>{const move=(e:PointerEvent)=>{if(dragging.current)setFromClientX(e.clientX)};const up=()=>{dragging.current=false};addEventListener("pointermove",move);addEventListener("pointerup",up);return()=>{removeEventListener("pointermove",move);removeEventListener("pointerup",up)}},[setFromClientX]);return <section id="antes-depois" className="border-t border-line bg-bg-alt py-24 md:py-36 lg:py-44"><div className="shell"><Reveal className="grid grid-cols-1 gap-10 md:grid-cols-12 md:items-end"><div className="md:col-span-8"><SectionIndex index="06 / 08" label="Antes e depois"/><h2 className="mt-10 font-display text-[clamp(2.5rem,6vw,6rem)] font-extrabold uppercase leading-[0.88] tracking-[-0.045em]">A luz não<br/><span className="text-ink-dim">mente.</span></h2></div><p className="label max-w-xs md:col-span-4 md:justify-self-end">Arraste para comparar. O mesmo painel, antes e depois da correção.</p></Reveal></div><Reveal delay={120} className="shell mt-14 md:mt-20"><div ref={frameRef} onPointerDown={e=>{dragging.current=true;setFromClientX(e.clientX)}} className="comparison relative aspect-[4/5] w-full cursor-ew-resize touch-none select-none overflow-hidden sm:aspect-[16/9]"><img src={afterImage} alt="Capô escuro corrigido, com reflexo espelhado" width={1600} height={1000} loading="lazy" className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-y-0 left-0 overflow-hidden" style={{width:`${position}%`}}><img src={beforeImage} alt="Capô escuro opaco antes da correção" width={1600} height={1000} loading="lazy" className="h-full w-full max-w-none object-cover" style={{width:frameRef.current?.clientWidth??"100%"}}/></div><div className="absolute inset-x-0 top-5 flex justify-between px-5"><span className="label bg-bg/60 px-2 py-1 text-ink backdrop-blur-sm">Antes</span><span className="label bg-bg/60 px-2 py-1 text-ink backdrop-blur-sm">Depois</span></div><div className="pointer-events-none absolute inset-y-0 w-px bg-accent" style={{left:`${position}%`}}/><div className="pointer-events-none absolute top-1/2 grid h-14 w-14 -translate-x-1/2 -translate-y-1/2 place-items-center border border-accent bg-bg/80 backdrop-blur-sm" style={{left:`${position}%`}}><span className="font-mono text-[10px] tracking-[0.15em] text-accent">←→</span></div><input type="range" min="0" max="100" step="0.5" value={position} onChange={e=>setPosition(Number(e.target.value))} aria-label="Comparar antes e depois" className="absolute inset-0 h-full w-full cursor-ew-resize opacity-0"/></div></Reveal></section>;}